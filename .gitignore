# Virtual environment
.venv/

# Secrets
key.txt
config.env
*.pem

# Local databases (dev SQLite)
*.db
*.db-journal

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
.uv-cache/

# Data caches (large)
data/cache/
data/raw/
data/agent_memory/
*.parquet

# Manuscript files (kept locally, not tracked)
manuscript/

# Logs / runtime
*.log
logs/
replay/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Frontend (later)
node_modules/
.pnpm-store/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# Claude Code local settings
.claude/settings.local.json

# Mac launcher runtime (pid/log from scripts/start-all.sh)
.run/

# Runtime checkpoint state is generated locally. The two accepted V1 warm starts and their
# manifest are the only repository-owned checkpoint artifacts.
checkpoints/*
!checkpoints/manifest.v1.json
!checkpoints/bc_primitive_p2p_v1.pt
!checkpoints/bc_primitive_realprice_grid_v1.pt

# Backtest artifacts (generated; re-runnable via `./tasks.sh backtest`)
artifacts/

# Document render scratch space (generated by scripts/docs)
.docx_build/
